<%
    const props = {
        slide
    }
%>

<%- include( `${component}/card/testimonial`, {
    text: slide.text,
    avatar: {
        src: slide.avatar.src,
        alt: slide.avatar.alt,
        name: slide.avatar.name,
        job: slide.avatar.job,
    },
    stars: slide.stars,
} ) %>