<%

const props = {
    slide
}
%>

<%- include( `${component}/card/basic`, {
    img: {
        src: slide.img.src,
        alt: slide.img.alt
    },
    title: slide.title,
    text: slide.text
} ) %>