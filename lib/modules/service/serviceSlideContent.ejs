<%
    const props = {
        slide
    }
%>

<%- include( `${component}/card/service`, {
    titleService: slide.title,
    text: slide.text,
    tag: slide.tag,
    img: {
        src: slide.img.src,
        alt: slide.img.alt,
    },
    link: {
        href: slide.link.href,
        label: slide.link.label,
        icon: slide.link.icon
    }
}) %>