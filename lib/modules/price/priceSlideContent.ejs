<%
    const props = {
        slide
    }
%>

<%- include( `${component}/card/comparative`, {
    price: slide.price,
    icon: slide.icon,
    titleComparative: slide.title,
    text: slide.text,
    link: {
        href: slide.link.href,
        label: slide.link.label,
        icon: slide.link.icon
    },
    typesListElements: slide.typesListElements,
    hasBackgroundCard: slide.hasBackgroundCard,
    hasBorderCard: slide.hasBorderCard,
    hasShadowCard: slide.hasShadowCard
} ) %>