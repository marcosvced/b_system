<%

const props = {
    slide
}
%>

<%- include( `${component}/card/basic`, {
    img: {
        src: slide.img.src,
        alt: slide.img.alt
    },
    avatar: {
        src: slide.avatar.src,
        alt: slide.avatar.alt
    },
    title: slide.title,
    text: slide.text
} ) %>