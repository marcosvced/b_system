<%
    const props = {
        grid,
        hasBorder
    }

    const styles = {
        portfolioGrid: {
            border: {
            toString: () => (props.hasBorder ? 'section-divider' : '')
            }
        }
    }
%>

<div class="portfolio module <%- styles.portfolioGrid.border -%> d-grid grid-1 grid-xl-2 gap-0 p-0">
    <% props.grid.forEach((project)=> { %>
        <%- include( `${component}/portfolio/portfolio`, {
            img: {
                src: project.img.src,
                srcMobile: project.img.srcMobile,
                alt: project.img.alt
            },
            tag: project.tag,
            title: project.title,
            text: project.text,
            link: {
                href: project.link.href,
                label: project.link.label,
                icon: project.link.icon
            },
            shape: project.shape,
            contentPosition: project.contentPosition
        } ) %>
    <% }) %>
</div>