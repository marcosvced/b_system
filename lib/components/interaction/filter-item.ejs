<%
    /**
	* @required {string} label
	*/

    const props = {
        label: label,
        href: typeof href !== 'undefined' ? href : '',
    }
%>

<% if ( '' !== props.href ) { %>
    <a href="<%= props.href %>"><%= props.label %></a>
<% } else { %>
    <%= props.label %>
<% } %>
