<%
/**
 * @required { src:String, size:String, alt:String } img
 */

  const props = {
    img: img,
  }

%>

  <picture class="illustration-container">
    <img class="illustration-img" src="<%- `${image}/${props.img.size}/${props.img.src}.svg` -%>" alt="<%- props.img.alt -%>">
  </picture>