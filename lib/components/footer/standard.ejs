<%
/**
 * @required { src:String, alt:String } img
 */

const props = {
    img: img,
    text: typeof text !== 'undefined' ? text : null,
    isDarkened: typeof isDarkened !== 'undefined' ? isDarkened : false
}

const styles = {
    footer: {
        darkened: {
            toString: () => (props.isDarkened ? 'darkened' : '')
        }
    }
}

%>

<footer class="footer footer-estandar <%- styles.footer.darkened -%> inner-space-inline-16 inner-space-inline-sm-24">
    <div class="footer-wrapper container">
        <div class="footer-content d-flex gap-32 inner-space-block-48 inner-space-block-md-56 inner-space-block-xl-72">
            <a class="footer-logo" href="<%= QUICK_START.HOME %>">
                <img src="<%- `${image}/${props.img.src}` -%>" class="logo" alt="<%- props.img.alt -%>" />
            </a>
            <p class="copy">
                <%= props.text %>
            </p>
        </div>
    </div>
</footer>
